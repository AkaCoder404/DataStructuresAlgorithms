/*
 * @lc app=leetcode.cn id=628 lang=cpp
 *
 * [628] 三个数的最大乘积
 */

// @lc code=start
class Solution {
public:
    int maximumProduct(vector<int>& nums) {
        // this only gives us largest number for all positives
        // int first = -1001;
        // int second = -1001;
        // int third = -1001;
        // int first_loc = -1;
        // int second_loc = -1;
        // int third_loc = -1;

        // for(int i = 0; i < nums.size(); i++) {
        //     if (first < nums[i]) {
        //         first = nums[i];
        //         first_loc = i;
        //     }
        // }

        // for(int i = 0; i < nums.size(); i++) {
        //     if (second <= nums[i] && nums[i] <= first && i != first_loc) {
        //         second = nums[i];
        //         second_loc = i;
        //     }

        // } 

        // for(int i = 0; i < nums.size(); i++) {
        //     if (third <= nums[i] && nums[i] <= second && nums[i] <= first && i != first_loc && i != second_loc) {
        //         third = nums[i];
        //     }
        // }

        // return first * second * third;





    }
};
// @lc code=end

